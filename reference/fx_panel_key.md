# Collapse functional_markers' (block, population, marker) into one panel key, shared by fig_functional_markers() and the stats call in main() so a figure's bracket always matches the right row in functional_markers_stats.csv

Collapse functional_markers' (block, population, marker) into one panel
key, shared by fig_functional_markers() and the stats call in main() so
a figure's bracket always matches the right row in
functional_markers_stats.csv

## Usage

``` r
fx_panel_key(fx)
```

## Arguments

- fx:

  The fx.
