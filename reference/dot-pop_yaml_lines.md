# Render one population's marker requirements as config YAML lines

Render one population's marker requirements as config YAML lines

## Usage

``` r
.pop_yaml_lines(name, req, comment = NULL)
```

## Arguments

- name:

  Population name.

- req:

  Named list of marker -\> direction.

- comment:

  Optional comment line placed above the entry.
