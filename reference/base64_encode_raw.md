# Encode a raw vector as base64

Encode a raw vector as base64

## Usage

``` r
base64_encode_raw(r)
```

## Arguments

- r:

  A raw vector.

## Value

A single character string, padded with `=` as the standard requires.
