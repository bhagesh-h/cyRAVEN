# The label assigned to cells inside the parent gate matching no population

The label assigned to cells inside the parent gate matching no
population

## Usage

``` r
catch_all_label()
```

## Value

A single string.
