# Arcsine square root transform for percentages

Maps `[0, 100]` onto `[0, pi/2]`. Variance-stabilising for proportion
data.

## Usage

``` r
asin_sqrt_pct(pct)
```

## Arguments

- pct:

  Numeric vector of percentages.

## Value

Numeric vector on the arcsine scale.
