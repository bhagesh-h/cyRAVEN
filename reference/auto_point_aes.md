# Point size / alpha that stay legible across two orders of magnitude of N WHY: a size that reads well at 200k cells is invisible at 3k, and vice versa. Fixed values are the most common cause of unreadable UMAP panels.

Point size / alpha that stay legible across two orders of magnitude of N
WHY: a size that reads well at 200k cells is invisible at 3k, and vice
versa. Fixed values are the most common cause of unreadable UMAP panels.

## Usage

``` r
auto_point_aes(n)
```

## Arguments

- n:

  Number of items.
