# The sample ids drawn at one timepoint

The sample ids drawn at one timepoint

## Usage

``` r
samples_at_timepoint(smap, level)
```

## Arguments

- smap:

  Sample map.

- level:

  One timepoint level.

## Value

Character vector of sample ids.
