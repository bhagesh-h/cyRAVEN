# The three monocyte subsets, named exactly as in the population spec. WHY a named helper: the document scopes functional blocks as "every subset EXCEPT the monocytes" and "the monocyte subsets only", so both blocks need the same list and it must stay consistent if the spec is edited.

The three monocyte subsets, named exactly as in the population spec. WHY
a named helper: the document scopes functional blocks as "every subset
EXCEPT the monocytes" and "the monocyte subsets only", so both blocks
need the same list and it must stay consistent if the spec is edited.

## Usage

``` r
monocyte_populations()
```
