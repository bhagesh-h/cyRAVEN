# Write a draft population specification from the clusters

Closes the loop: discovery becomes a declaration you can curate and
re-run through the supervised path, where it acquires per-sample
thresholds, uncertainty and the concordance checks. Never adopted
automatically.

## Usage

``` r
explore_write_spec(pos, prof, ks, ex_dir, tag = "")
```
