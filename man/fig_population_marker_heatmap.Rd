% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/state-heatmaps.R
\name{fig_population_marker_heatmap}
\alias{fig_population_marker_heatmap}
\title{Population x marker phenotype heatmap}
\usage{
fig_population_marker_heatmap(
  mfi,
  outfile,
  scale_by = c("marker", "none"),
  min_cells = 20L,
  annotate_expected = NULL,
  panel_label = "",
  dpi = 300,
  colors = fcs_colors()
)
}
\arguments{
\item{mfi}{population_marker_mfi table}

\item{outfile}{Path to write the figure to.}

\item{scale_by}{"marker" (row z-score, the annotation/validation view),
"none" (raw arcsinh medians, comparable across populations within a marker
but not between markers)}

\item{min_cells}{per-sample floor before a sample contributes to a cell}

\item{annotate_expected}{optional named list population -> character vector of
markers the gate definition REQUIRES to be positive. Drawn as an outline on
those cells, turning the figure into an explicit gate audit.}

\item{panel_label}{Marker-panel name added to the figure title; empty for none. Default \code{""}.}

\item{dpi}{Resolution in dots per inch. May be reduced automatically to respect the raster ceiling; see \code{\link[=safe_ggsave]{safe_ggsave()}}. Default \code{300}.}

\item{colors}{Named list of colours; defaults to the package palette. See \code{\link[=fcs_colors]{fcs_colors()}}. Default \code{fcs_colors()}.}
}
\description{
Population x marker phenotype heatmap
}
