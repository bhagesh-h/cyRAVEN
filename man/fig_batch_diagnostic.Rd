% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics.R
\name{fig_batch_diagnostic}
\alias{fig_batch_diagnostic}
\title{Batch diagnostic figure: the embedding coloured by batch, plus the LISI
distribution against its permutation null}
\usage{
fig_batch_diagnostic(
  cells,
  report,
  outfile,
  batch_col,
  panel_label = "",
  dpi = 200,
  colors = fcs_colors()
)
}
\arguments{
\item{cells}{Embedded cell table: one row per cell, carrying \code{umap_1}/\code{umap_2}, \code{sample_id}, \code{population_label} and one column per marker.}

\item{report}{The report.}

\item{outfile}{Path to write the figure to.}

\item{batch_col}{Column naming the acquisition batch.}

\item{panel_label}{Marker-panel name added to the figure title; empty for none. Default \code{""}.}

\item{dpi}{Resolution in dots per inch. May be reduced automatically to respect the raster ceiling; see \code{\link[=safe_ggsave]{safe_ggsave()}}. Default \code{200}.}

\item{colors}{Named list of colours; defaults to the package palette. See \code{\link[=fcs_colors]{fcs_colors()}}. Default \code{fcs_colors()}.}
}
\description{
WHY BOTH PANELS: the scatter is what a reader will look at anyway and it shows
WHERE any batch structure sits; the LISI distribution is what says whether the
pattern the eye finds is more than chance. Neither alone is enough -- a
scattered-looking plot can still be significantly structured at these n, and a
significant score with no visible pattern is usually one small batch.
}
