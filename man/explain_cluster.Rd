% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/convex-gates.R
\name{explain_cluster}
\alias{explain_cluster}
\title{Learn a hierarchical gating strategy for one labelled population}
\usage{
explain_cluster(X, y, max_depth = 4L, min_gain = 0.02, markers = NULL, ...)
}
\arguments{
\item{X}{n x m matrix of marker values (arcsinh scale)}

\item{y}{0/1 or logical target indicator}

\item{max_depth}{maximum gates in the strategy}

\item{min_gain}{minimum held-out F1 improvement to justify another level}

\item{markers}{optional restriction of the marker set}

\item{...}{passed to \code{\link[=learn_convex_gate]{learn_convex_gate()}}}
}
\value{
list(levels = list of gates, summary = data.frame, best_depth), or NULL
}
\description{
WHAT: repeatedly picks the two most discriminating markers among the cells
that survive so far, fits a convex gate in that plane, keeps the cells inside
it, and goes again -- which is exactly the shape of a manual gating strategy,
and is why the output can be executed by hand.
}
\details{
WHY IT STOPS EARLY. Each additional level can only remove cells, so recall
falls monotonically while precision usually rises. The strategy is worth
extending only while F1 improves; \code{min_gain} sets how much improvement counts
as worth another gate for whoever has to draw it. It also stops when a level
fails to fit, rather than skipping it -- a gap in the middle of a hierarchy
is not a gating strategy, and returning the levels that did work is the
honest truncation.
}
