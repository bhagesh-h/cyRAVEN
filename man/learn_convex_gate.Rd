% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/convex-gates.R
\name{learn_convex_gate}
\alias{learn_convex_gate}
\title{Learn a convex two-marker gate for a labelled set of cells}
\usage{
learn_convex_gate(
  X,
  y,
  n_planes = 8L,
  s_schedule = 800,
  lambda_grid = c(0, 0.5, 1, 2, 4, 8),
  holdout = 0.3,
  n_target_total = NULL,
  max_cells = 20000L,
  seed = 42L
)
}
\arguments{
\item{X}{n x 2 numeric matrix of marker values (arcsinh scale)}

\item{y}{0/1 or logical target indicator, length n}

\item{n_planes}{total half-planes; 4 are PC-aligned and fixed, the rest free}

\item{s_schedule}{sigmoid steepness. Supplying several increasing values
anneals: each fit warm-starts the next. The default is a single value,
because annealing measured no reliable improvement (+0.002 to +0.007 F1 on
the cases tried, inside the seed-to-seed spread) for roughly twice the
runtime. It is left available for a dataset where the fit does get stuck}

\item{lambda_grid}{tightness values to search}

\item{holdout}{fraction of cells reserved for evaluation, stratified by label}

\item{n_target_total}{recall denominator; defaults to the targets supplied}

\item{max_cells}{subsample ceiling for the fit}

\item{seed}{RNG seed; the stream is restored on exit}
}
\value{
list with \code{polygon}, \code{hull}, \code{metrics}, \code{metrics_hull},
\code{metrics_insample}, \code{lambda}, \code{W}, \code{b}, \code{markers}, or NULL if no gate could
be fitted
}
\description{
WHAT: fits an intersection of half-planes that separates \code{y == 1} cells from
the rest in the plane of two markers, then reports how well it does on cells
held out of the fit.
}
\details{
HOW THE TIGHTNESS IS CHOSEN: \code{lambda} is not a tuning parameter the caller has
to guess. The function fits at each value of \code{lambda_grid}, scores every fit
on held-out cells, and keeps the best; it then refines once around the winner.
Searching against held-out F1 rather than training F1 is what stops the search
from always selecting the loosest gate, which is what fits the training cells
best and generalises worst.

WHY IT RETURNS TWO POLYGONS. \code{polygon} is the fitted half-plane intersection.
\code{hull} is the convex hull of the target cells that fell inside it -- strictly
tighter, usually more precise, and much more prone to memorising the training
cells, since it is drawn around them. Both are returned with their own
held-out metrics so the trade is visible instead of being made silently.
}
