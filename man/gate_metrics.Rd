% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/convex-gates.R
\name{gate_metrics}
\alias{gate_metrics}
\title{Precision, recall and F1 for a binary gate}
\usage{
gate_metrics(inside, y, n_target_total = sum(y == 1))
}
\arguments{
\item{inside}{logical, gate membership}

\item{y}{0/1 target indicator}

\item{n_target_total}{denominator for recall}
}
\value{
named numeric vector
}
\description{
Recall is measured against \code{n_target_total} rather than against the targets
present, so that in a hierarchy it stays CUMULATIVE: a level-3 gate is judged
on the fraction of the ORIGINAL population it still holds, not on the
fraction of whatever survived level 2. A hierarchy that discards half the
population at each level and reports 100\% recall three times is exactly the
failure this guards against.
}
\keyword{internal}
