% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/input-check.R
\name{list_channels}
\alias{list_channels}
\title{List the acquisition parameters and the names the run resolves them to}
\usage{
list_channels(fcs)
}
\arguments{
\item{fcs}{Paths to the FCS files.}
}
\value{
invisible data.frame of the reference file's parameters.
}
\description{
One row per parameter: index, the detector name (\verb{$PnN}), the description
(\verb{$PnS}), the symbol the run resolves it to, and how the run will use it. The
resolved symbol is the string a population specification has to name.
}
\details{
Reads headers only, so it costs nothing on large files, and reads every file
rather than the first so a panel that differs between files is reported.
}
\keyword{internal}
