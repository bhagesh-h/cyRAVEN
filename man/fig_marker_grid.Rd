% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/figures.R
\name{fig_marker_grid}
\alias{fig_marker_grid}
\title{Marker-expression grid over the shared embedding}
\usage{
fig_marker_grid(
  cells,
  markers,
  outfile,
  panel_label = "",
  ncol = NULL,
  dpi = 300,
  panel_size = 2.5,
  colors = fcs_colors()
)
}
\arguments{
\item{cells}{data.frame with umap_1/umap_2 plus one column per marker.}

\item{markers}{character vector of marker column names to render.}

\item{outfile}{Path to write the figure to.}

\item{panel_label}{Marker-panel name added to the figure title; empty for none. Default \code{""}.}

\item{ncol}{Number of panel columns; NULL computes one that keeps the canvas roughly square.}

\item{dpi}{Resolution in dots per inch. May be reduced automatically to respect the raster ceiling; see \code{\link[=safe_ggsave]{safe_ggsave()}}. Default \code{300}.}

\item{panel_size}{The panel size. Default \code{2.5}.}

\item{colors}{Named list of colours; defaults to the package palette. See \code{\link[=fcs_colors]{fcs_colors()}}. Default \code{fcs_colors()}.}
}
\description{
WHAT: one small UMAP per marker, coloured by that marker's asinh intensity.
WHY:  this is the check that the embedding is biologically structured -- each
lineage marker should light up a coherent region. If markers are
smeared uniformly, the embedding is driven by noise (which is exactly
what the ungated template produced).
}
