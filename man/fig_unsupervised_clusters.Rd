% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clustering.R
\name{fig_unsupervised_clusters}
\alias{fig_unsupervised_clusters}
\title{Unsupervised-clustering figure: the embedding by cluster, next to the same
embedding by gate label}
\usage{
fig_unsupervised_clusters(
  cells,
  cluster,
  outfile,
  agreement = NULL,
  panel_label = "",
  dpi = 200,
  colors = fcs_colors()
)
}
\arguments{
\item{cells}{Embedded cell table: one row per cell, carrying \code{umap_1}/\code{umap_2}, \code{sample_id}, \code{population_label} and one column per marker.}

\item{cluster}{Integer vector of cluster assignments, one per cell.}

\item{outfile}{Path to write the figure to.}

\item{agreement}{The agreement.}

\item{panel_label}{Marker-panel name added to the figure title; empty for none. Default \code{""}.}

\item{dpi}{Resolution in dots per inch. May be reduced automatically to respect the raster ceiling; see \code{\link[=safe_ggsave]{safe_ggsave()}}. Default \code{200}.}

\item{colors}{Named list of colours; defaults to the package palette. See \code{\link[=fcs_colors]{fcs_colors()}}. Default \code{fcs_colors()}.}
}
\description{
WHY SIDE BY SIDE AND NOT TWO FILES: the entire value is the comparison. A
cluster panel alone is a pretty picture; against the gate panel it is an
audit, and a reader can see in one glance which islands the spec named and
which it missed.
}
