% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stats-abundance.R
\name{fig_group_comparison}
\alias{fig_group_comparison}
\title{Grouped abundance figure: one panel per population, bar + SD + every sample}
\usage{
fig_group_comparison(
  freq,
  outfile,
  group_of,
  stats = NULL,
  reference = NULL,
  p_source = c("raw", "BH"),
  measure = "auto",
  ncol = 3L,
  panel_label = "",
  dpi = 300,
  value_col = NULL,
  value_label = NULL,
  value_caveat = NULL,
  title_noun = "Population abundance",
  colors = fcs_colors()
)
}
\arguments{
\item{freq}{population_frequencies table}

\item{outfile}{Path to write the figure to.}

\item{group_of}{named vector sample_id -> group}

\item{stats}{output of stats_group_comparison(); NULL to skip brackets}

\item{reference}{The group every other group is compared against.}

\item{p_source}{"raw" or "BH" -- which p-value the brackets display}

\item{measure}{Which abundance measure to use. Default \code{"auto"}.}

\item{ncol}{Number of panel columns; NULL computes one that keeps the canvas roughly square. Default \code{3L}.}

\item{panel_label}{Marker-panel name added to the figure title; empty for none. Default \code{""}.}

\item{dpi}{Resolution in dots per inch. May be reduced automatically to respect the raster ceiling; see \code{\link[=safe_ggsave]{safe_ggsave()}}. Default \code{300}.}

\item{value_col, value_label, value_caveat}{override the abundance measure
with an arbitrary numeric column of \code{freq} (must still be one row per
sample_id x population). Used to draw the same bar/median/whisker/points
layout, with the same Wilcoxon-bracket machinery, for tables that share
\code{freq}'s shape but not its meaning -- functional-marker positivity, or a
derived population ratio.}

\item{title_noun}{the figure title's subject, e.g. "Population abundance"
or "Functional marker positivity"; " by group" is appended automatically
when 2+ groups are plotted.}

\item{colors}{Named list of colours; defaults to the package palette. See \code{\link[=fcs_colors]{fcs_colors()}}. Default \code{fcs_colors()}.}
}
\description{
WHAT: mean bar, SD whiskers, one point per sample, significance brackets from
the reference group to each other group. Panels are lettered.
}
\details{
WHY POINTS AND NOT JUST BARS: at n = 6-11 a bar and a whisker hide whether a
"difference" rests on one outlier or on a consistent shift. Every sample is
drawn, so the reader can see the shape of the evidence -- which is also why the
bar is left unfilled for the reference group: the fill carries group identity,
not emphasis.
}
