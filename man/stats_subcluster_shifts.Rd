% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/figures.R
\name{stats_subcluster_shifts}
\alias{stats_subcluster_shifts}
\title{Per (cluster, subcluster, marker, study): how far is this study from reference}
\usage{
stats_subcluster_shifts(
  cells,
  markers,
  subcluster,
  group_col = "cohort",
  reference = NULL,
  min_cells = 20L
)
}
\arguments{
\item{cells}{Embedded cell table: one row per cell, carrying \code{umap_1}/\code{umap_2}, \code{sample_id}, \code{population_label} and one column per marker.}

\item{markers}{Character vector of marker names to use.}

\item{subcluster}{Integer vector of subcluster assignments, one per cell.}

\item{group_col}{Name of the column holding the biological grouping (cohort). Default \code{"cohort"}.}

\item{reference}{The group every other group is compared against.}

\item{min_cells}{Minimum cells a sample must contribute before it is used. Default \code{20L}.}
}
\description{
The figure shows the shift; this makes it sortable. Reports BOTH deviation
modes named above -- the occupancy of the subcluster and the intensity of the
marker within it -- so "which markers and which cells differ" is a sort on a
column rather than an eyeball over 288 panels.
}
\details{
Effect size is the median difference in asinh units plus Cliff's delta, which
is bounded �-1 to 1 and rank-based: at these per-subcluster n a difference in
means is dominated by outliers, and these distributions are not normal.
}
