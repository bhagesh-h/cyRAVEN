% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/batch-correct.R
\name{correct_batch}
\alias{correct_batch}
\title{Correct batch effects in the transformed marker matrix}
\usage{
correct_batch(
  tmat,
  batch,
  group = NULL,
  markers = NULL,
  max_cramers_v = 0.6,
  force = FALSE
)
}
\arguments{
\item{tmat}{numeric matrix, cells x markers, already transformed}

\item{batch}{batch label per cell}

\item{group}{biological group per cell, for the confounding check}

\item{markers}{markers to correct; defaults to all columns}

\item{max_cramers_v}{refusal threshold}

\item{force}{proceed despite confounding}
}
\value{
list(tmat, cramers_v, corrected, markers, reason)
}
\description{
WHAT IT DOES: aligns each marker's distribution across batches, after
checking that batch and biological group are separable enough for that to be
meaningful.
}
\details{
WHAT IT REFUSES TO DO: correct when Cramer's V between batch and group
exceeds \code{max_cramers_v}. At that point the two are close to the same
variable, and any correction removes the effect being looked for. Passing
\code{force = TRUE} proceeds and records the decision in the returned object so it
reaches the run manifest.

This is descriptive of intensity only. It does not touch the gate hierarchy,
which is derived per sample and is already batch-local by construction.
}
