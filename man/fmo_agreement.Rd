% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fmo.R
\name{fmo_agreement}
\alias{fmo_agreement}
\title{Distance between a derived cut and its FMO-anchored equivalent}
\usage{
fmo_agreement(
  thr_all,
  fmo_thresholds,
  unc = NULL,
  agree_at = 1,
  disagree_at = 3
)
}
\arguments{
\item{thr_all}{the thresholds table, carrying \code{sample_id}, \code{marker},
\code{threshold} and \code{source}}

\item{fmo_thresholds}{data.frame of \code{sample_id}, \code{marker}, \code{fmo_threshold},
\code{fmo_sample}}

\item{unc}{optional \code{thresholds} element of \code{\link[=run_gate_uncertainty]{run_gate_uncertainty()}}, used to
scale the distance}

\item{agree_at}{distance in uncertainties within which the two agree}

\item{disagree_at}{distance beyond which they are reported as disagreeing}
}
\value{
a data.frame, or NULL
}
\description{
The diagnostic the feature exists for. Positive means the sample's own density
put the cut above the control; negative means below.
}
\details{
HOW TO READ \code{distance_in_u}. It is the gap expressed in units of that
threshold's own standard uncertainty, from \code{threshold_uncertainty.csv}. Within
about one, the two methods agree to the precision either can claim, and the
derived cut is corroborated by an independent experiment. Beyond about three,
they disagree by more than either can explain and one of them is wrong: a
derived cut far above the FMO is discarding real signal, and one far below is
calling spillover positive.
}
