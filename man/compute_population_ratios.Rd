% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stats-abundance.R
\name{compute_population_ratios}
\alias{compute_population_ratios}
\title{Derived abundance ratios (e.g. CD4:CD8), from the config's \verb{ratios:} block}
\usage{
compute_population_ratios(freq, ratios)
}
\arguments{
\item{freq}{population_frequencies table (sample_id, population,
pct_of_cd45_pos, is_control)}

\item{ratios}{named list from cfg$ratios; each entry has \code{numerator} and
\code{denominator} (population names from \verb{populations:}) and an optional
\code{label}}
}
\value{
data.frame(sample_id, ratio, population (=label), numerator,
denominator, numerator_pct, denominator_pct, value, is_control), or NULL
}
\description{
WHY CONFIG-DRIVEN, NOT HARDCODED: population names live in \verb{populations:}
(section 5.4) and are study-specific -- a ratio hardcoded to "CD4 T cells"/"CD8 T
cells" would silently mean nothing (or error) under a config using
different labels. Each ratio supplies its own numerator/denominator
population name, the same way each functional_blocks entry supplies its
own marker list, so this stays generic across cohorts and panels.
}
