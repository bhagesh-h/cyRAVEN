% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/state-heatmaps.R
\name{fig_cohort_confusion}
\alias{fig_cohort_confusion}
\title{Cohort composition heatmap ("confusion matrix")}
\usage{
fig_cohort_confusion(
  cells,
  outfile,
  group_col = "cohort",
  pop_col = "population_label",
  norm_to = 1000,
  panel_label = "",
  dpi = 300,
  colors = fcs_colors()
)
}
\arguments{
\item{cells}{Embedded cell table: one row per cell, carrying \code{umap_1}/\code{umap_2}, \code{sample_id}, \code{population_label} and one column per marker.}

\item{outfile}{Path to write the figure to.}

\item{group_col}{Name of the column holding the biological grouping (cohort). Default \code{"cohort"}.}

\item{pop_col}{Name of the column holding the population label. Default \code{"population_label"}.}

\item{norm_to}{Notional cell count each group is normalised to before shares are taken. Default \code{1000}.}

\item{panel_label}{Marker-panel name added to the figure title; empty for none. Default \code{""}.}

\item{dpi}{Resolution in dots per inch. May be reduced automatically to respect the raster ceiling; see \code{\link[=safe_ggsave]{safe_ggsave()}}. Default \code{300}.}

\item{colors}{Named list of colours; defaults to the package palette. See \code{\link[=fcs_colors]{fcs_colors()}}. Default \code{fcs_colors()}.}
}
\description{
WHAT: for each population, the share of its cells contributed by each cohort
AFTER equalising every cohort to the same notional cell count.
}
\details{
WHY THE EQUALISATION IS THE WHOLE POINT: cohorts differ in sample count and in
acquisition depth, so raw contributions are dominated by whichever cohort has
the most cells and every population looks like it belongs to that cohort. The
normalisation -- the same device used by the cohort-confusion heatmaps
elsewhere in the field, which equalise to a fixed cell count per group --
removes that, so a row that is not near-uniform means a population is
genuinely cohort-skewed.

WHY IT IS NOT A SUBSTITUTE FOR group_comparison.png: this pools cells across
donors, so it shows a pattern, not a tested effect. A population that looks
skewed here is a lead to check against the sample-level abundance test, which
is the figure that carries statistics.
}
