% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read-fcs.R
\name{marker_symbol}
\alias{marker_symbol}
\title{The marker name cyRAVEN resolves from an FCS description}
\usage{
marker_symbol(desc, nm = NULL)
}
\arguments{
\item{desc}{\verb{$PnS} values.}

\item{nm}{\verb{$PnN} values, used where \code{desc} is empty.}
}
\value{
Character vector of resolved marker names.
}
\description{
Spectral instruments write \verb{$PnS} as \code{"CD45 : SparkUV-387 - Area"}, the
antibody, the detector, and the pulse statistic in one string. The name a
population specification has to use is the antibody alone, so everything from
\code{" : "} onward is stripped. Where \verb{$PnS} is empty the channel name \verb{$PnN}
stands in.
}
\details{
EXTRACTED SO IT CANNOT DIVERGE. \code{\link[=read_fcs_resolved]{read_fcs_resolved()}} and
\code{\link[=report_input_check]{report_input_check()}} both need it, and for a while only the first had it:
\code{--check} then reported markers as the full \verb{$PnS} string and told the user
that every population named a marker no file contained, on data where the run
itself would have resolved all of them. A false alarm from the one command
whose whole purpose is to catch real ones.
}
\keyword{internal}
