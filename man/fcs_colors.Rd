% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/zzz.R
\name{fcs_colors}
\alias{fcs_colors}
\alias{set_fcs_colors}
\title{The colour palette every figure draws with}
\usage{
fcs_colors()

set_fcs_colors(colors)
}
\arguments{
\item{colors}{Named list of colours; defaults to the package palette. See \code{\link[=fcs_colors]{fcs_colors()}}.}
}
\value{
\code{fcs_colors()} returns the active named list of colours.
\code{set_fcs_colors()} returns the previous value invisibly, so it can be
restored with \code{\link[=on.exit]{on.exit()}}.
}
\description{
\code{fcs_colors()} returns the active palette; \code{set_fcs_colors()} replaces it,
merging over the built-in defaults so a partial specification does not blank
out the keys it omits.
}
\details{
Held in a private package environment rather than a global variable, so that
re-theming a run cannot leak into another session and the state can be
restored (\code{set_fcs_colors(NULL)}).

\code{NULL} to restore the defaults.
}
\examples{
old <- set_fcs_colors(list(gate_highlight = "#0072F0"))
fcs_colors()$gate_highlight
set_fcs_colors(old)
}
\seealso{
\code{\link[=default_colors]{default_colors()}} for the full set of keys and what each controls.
}
