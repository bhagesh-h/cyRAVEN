% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gate-autofix.R
\name{gate_needs_autofix}
\alias{gate_needs_autofix}
\title{Decide whether a hierarchy gate rests on evidence or on the fallback constant}
\usage{
gate_needs_autofix(
  source,
  enabled = FALSE,
  kept = NA_real_,
  parent = NA_real_,
  min_retention = 0.05
)
}
\arguments{
\item{source}{The \code{source} string \code{resolve_threshold()} returned.}

\item{enabled}{Whether \code{--auto-fix-gates} is set.}
}
\value{
\code{list(skip, reason)}. \code{skip = TRUE} means apply no cut at this gate.
}
\description{
Separated from \code{apply_gate_hierarchy()} so the decision is testable on its
own and so the reason travels with it as text, rather than being implied by
a threshold silently becoming NA.
}
\keyword{internal}
