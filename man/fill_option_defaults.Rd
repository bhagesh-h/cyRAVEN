% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipeline.R
\name{fill_option_defaults}
\alias{fill_option_defaults}
\title{Fill an option list from the command-line defaults}
\usage{
fill_option_defaults(opt)
}
\arguments{
\item{opt}{Named list, possibly partial.}
}
\value{
\code{opt} with every unset option filled from its command-line default.
}
\description{
WHY THIS EXISTS. \code{run_cyraven()} is documented -- in the README, in the
vignette, in its own examples -- as taking a PARTIAL list: name the two or
three options that matter and let the rest default. That only worked when the
list arrived from \code{optparse}, which supplies every default itself. Called
directly with a short list, every unnamed option was NULL, and the failures
that produced were silent and remote from their cause: \code{singlet_mad_k}
missing made the singlet band \verb{median +/- NULL * mad}, which selects no
events, so every marker threshold resolved over an empty vector to NA, and
the run reported that every population's markers were "not in panel" --
naming the panel for a fault in the caller's option list.
}
\details{
Taking the defaults from \code{build_option_list()} rather than restating them
means there is one place a default is written down, and the programmatic and
command-line paths cannot drift apart.
}
\keyword{internal}
