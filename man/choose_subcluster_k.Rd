% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clustering.R
\name{choose_subcluster_k}
\alias{choose_subcluster_k}
\title{Choose k per population from the reference group's cells}
\usage{
choose_subcluster_k(
  cells,
  markers,
  group_col = "cohort",
  reference = NULL,
  k_range = 2:5,
  min_ref = 150L,
  seed = 42L
)
}
\arguments{
\item{cells}{Embedded cell table: one row per cell, carrying \code{umap_1}/\code{umap_2}, \code{sample_id}, \code{population_label} and one column per marker.}

\item{markers}{Character vector of marker names to use.}

\item{group_col}{Name of the column holding the biological grouping (cohort). Default \code{"cohort"}.}

\item{reference}{The group every other group is compared against.}

\item{k_range}{Candidate values of k to score. Default \code{2:5}.}

\item{min_ref}{Minimum reference-group cells required to fit. Default \code{150L}.}

\item{seed}{Random seed. The RNG stream is restored afterwards, so this cannot perturb later sampling. Default \code{42L}.}
}
\value{
list(k = named integer population -> k, curve = data.frame of scores)
}
\description{
Choose k per population from the reference group's cells
}
