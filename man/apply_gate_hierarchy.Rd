% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gating.R
\name{apply_gate_hierarchy}
\alias{apply_gate_hierarchy}
\title{Apply the full gate hierarchy to one file}
\usage{
apply_gate_hierarchy(
  rd,
  cofactor,
  cfg = list(),
  control_ref = NULL,
  singlet_k = 3,
  viability_name = NULL,
  cd45_name = "CD45",
  transform = NULL,
  overrides = NULL,
  autofix = FALSE,
  adaptive = FALSE
)
}
\arguments{
\item{rd}{The rd.}

\item{cofactor}{Arcsinh cofactor. See \code{\link[=derive_cofactor]{derive_cofactor()}}.}

\item{cfg}{The cfg. Default \code{list()}.}

\item{control_ref}{The control ref.}

\item{singlet_k}{The singlet k. Default \code{3}.}

\item{viability_name}{The viability name.}

\item{cd45_name}{The cd45 name. Default \code{"CD45"}.}

\item{transform}{Intensity transform from \code{\link[=make_transform]{make_transform()}}. Defaults to
arcsinh with \code{cofactor}, which is what every caller got before the
transform became selectable.}

\item{overrides}{Optional per-marker override entries for THIS sample, as
returned by indexing the config's \code{sample_overrides} block by sample id.
See \code{\link[=sample_override]{sample_override()}}. Absent by default, in which case every threshold is
derived exactly as before.}

\item{autofix}{Skip a hierarchy gate whose threshold came from
\code{quantile_fallback}, carrying its parent through unchanged. Such a gate
has its retention decided by the fallback constant rather than by the
data, so it keeps a fixed share of the parent whatever the sample holds.
\code{FALSE} by default, because skipping a gate changes every count in an
affected file. See \code{gate_needs_autofix()}.}

\item{adaptive}{Sweep the kernel bandwidth and try the tail and Otsu rules
before falling back to a quantile, for both hierarchy gates. See
\code{\link[=best_threshold]{best_threshold()}}. \code{FALSE} by default, because it moves thresholds.}
}
\value{
list of masks, derived geometry, thresholds, and a tidy counts table
}
\description{
Chain: all events -> leukocytes (scatter) -> single cells -> live -> CD45+.
Each step degrades gracefully: a missing viability dye or missing CD45 skips
that step with a warning rather than aborting, because a panel that lacks one
is still analysable.
}
