% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transform.R
\name{derive_cofactor}
\alias{derive_cofactor}
\title{Derive the arcsinh cofactor from the data}
\usage{
derive_cofactor(
  ex,
  marker_cols,
  target_iqr = 0.5,
  lo = 1,
  hi = 5000,
  max_markers = 40L
)
}
\arguments{
\item{ex}{Numeric expression matrix, events x channels.}

\item{marker_cols}{Named integer vector mapping marker symbol to column index.}

\item{target_iqr}{Interquartile range the transformed background is bisected towards. Default \code{0.5}.}

\item{lo}{Lower bound of the search interval. Default \code{1}.}

\item{hi}{Upper bound of the search interval. Default \code{5000}.}

\item{max_markers}{Ceiling on the number of markers used. Default \code{40L}.}
}
\description{
WHAT: for each marker, bisect the cofactor so the inter-quartile range of the
transformed background bulk equals \code{target_iqr}; the panel cofactor is the
median across markers.
WHY: the cofactor sets how much the near-zero region is expanded. Spectrally
unmixed data has a wide negative/background band; too small a cofactor (the
template's default of 5) expands that noise until it dominates all distances.
On the test batch this recovers ~150 for the baseline panel -- derived, not
assumed, so a different instrument or gain setting gets its own value.
}
