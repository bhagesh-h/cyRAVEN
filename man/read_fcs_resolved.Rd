% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read-fcs.R
\name{read_fcs_resolved}
\alias{read_fcs_resolved}
\title{Read one FCS file and resolve marker symbols}
\usage{
read_fcs_resolved(path, sample_id = NULL, max_events = 0L)
}
\arguments{
\item{path}{File path.}

\item{sample_id}{The sample id.}

\item{max_events}{The max events. Default \code{0L}.}
}
\value{
list(exprs, marker_cols, all_cols, keywords, n_events, file, sample_id)
}
\description{
WHY marker symbols, not channel names: detector/channel names are
instrument-specific and \code{alter.names = TRUE} mangles them (e.g. \verb{[[RB613]]-A}
becomes \code{X..RB613...A}). The biological identity lives in $PnS, which on this
instrument reads "CD45 : SparkUV-387 - Area" -- the symbol is the text before
" : ". Building an embedding on channel names (as the original template did)
means no marker identity ever reaches the analysis.
}
\details{
Note: FCS files larger than 99,999,999 bytes cannot express their data offset
in the 8-character HEADER field and write 0 there; the true offsets live in
the $BEGINDATA/$ENDDATA keywords. flowCore handles this fallback internally.
}
