% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transform-methods.R
\name{logicle_params_one}
\alias{logicle_params_one}
\title{Estimate logicle parameters for one marker}
\usage{
logicle_params_one(x, m = 4.5, a = 0, neg_q = 0.05)
}
\arguments{
\item{x}{numeric vector of raw intensities for one marker}

\item{m}{decades on the display scale}

\item{a}{additional negative decades to display}

\item{neg_q}{quantile of the negative values used as \code{r}}
}
\value{
list(w, t, m, a)
}
\description{
WHAT: the standard automatic-logicle rule (Parks, Roederer & Moore 2006;
Moore & Parks 2012). \code{t} is the top of scale, \code{m} the number of decades, and
\code{w} the width of the linear region, chosen so that the linear region just
covers the spread of the NEGATIVE population:
}
\details{
\if{html}{\out{<div class="sourceCode">}}\preformatted{w = (m - log10(t / |r|)) / 2
}\if{html}{\out{</div>}}

where \code{r} is a low quantile of the values below zero.

WHY A QUANTILE OF THE NEGATIVES AND NOT THE MINIMUM: the minimum is one event.
On a few hundred thousand events it is whatever the noisiest cell in the tube
did, and \code{w} derived from it stretches the linear region until the positive
decades are squashed. The 5th percentile of the negative values describes the
bulk of that population instead of its worst member.

Returns \code{w = 0} when a channel has no negative values at all -- with nothing
below zero there is nothing for a linear region to rescue, and logicle
degenerates to a log transform, which is the right answer rather than a
failure.
}
\keyword{internal}
