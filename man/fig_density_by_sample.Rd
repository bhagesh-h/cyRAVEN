% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/figures.R
\name{fig_density_by_sample}
\alias{fig_density_by_sample}
\title{Per-sample (or per-group) density comparison over the shared embedding}
\usage{
fig_density_by_sample(
  cells,
  outfile,
  panel_label = "",
  facet_by = "sample_id",
  dpi = 300,
  colors = fcs_colors()
)
}
\arguments{
\item{cells}{Embedded cell table: one row per cell, carrying \code{umap_1}/\code{umap_2}, \code{sample_id}, \code{population_label} and one column per marker.}

\item{outfile}{Path to write the figure to.}

\item{panel_label}{Marker-panel name added to the figure title; empty for none. Default \code{""}.}

\item{facet_by}{any column present in \code{cells} -- sample_id, cohort, or any
other covariate the patient table carried through.}

\item{dpi}{Resolution in dots per inch. May be reduced automatically to respect the raster ceiling; see \code{\link[=safe_ggsave]{safe_ggsave()}}. Default \code{300}.}

\item{colors}{Named list of colours; defaults to the package palette. See \code{\link[=fcs_colors]{fcs_colors()}}. Default \code{fcs_colors()}.}
}
\description{
WHY: side-by-side 2D density of the SAME embedding shows where each sample
or group gains or loses cells -- the comparison the shared embedding
exists for. Called with \code{facet_by = "sample_id"} (the default) for
per-sample QC, and again with the resolved group column when 2+ groups
exist, so cohorts can be compared the same way sample_id already is.
}
