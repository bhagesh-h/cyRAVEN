% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/marker-batch-drift.R
\name{marker_batch_drift}
\alias{marker_batch_drift}
\title{Per-marker distributional drift between acquisition batches}
\usage{
marker_batch_drift(
  cells,
  batch_col,
  markers = NULL,
  max_cells = 20000L,
  min_cells = 200L,
  flag_at = 0.5,
  seed = 42L
)
}
\arguments{
\item{cells}{cell-level table carrying \code{sample_id}, the batch column, and one
column per marker on the analysis scale}

\item{batch_col}{name of the batch column in \code{cells}}

\item{markers}{marker columns to test; defaults to every numeric column that
is not structural}

\item{max_cells}{cells sampled per batch before the distances are computed;
the quantile function stops moving long before the whole batch is used}

\item{min_cells}{smallest batch worth comparing}

\item{flag_at}{value of \code{emd_over_mad} at or above which a marker is flagged}

\item{seed}{seed for the local RNG stream, which is restored on exit}
}
\value{
a data.frame ordered by \code{emd_over_mad}, or NULL
}
\description{
For each marker, the largest Earth Mover's distance between any two batches,
reported in analysis units and scaled by the marker's pooled MAD so markers can
be compared with each other.
}
\details{
WHAT A FLAG MEANS. A marker whose distribution differs between batches by an
appreciable fraction of its own spread was measured differently in them. That
is a statement about the assay, not about the donors, unless batch and study
group coincide -- which is the question \code{batch_mixing_report()} answers with
Cramer's V, and which should be read first. Where the two overlap, this table
cannot separate a reagent lot from the biology either.
}
