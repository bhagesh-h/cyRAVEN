% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/acquisition-qc.R
\name{detect_time_anomalies}
\alias{detect_time_anomalies}
\title{Flag acquisition intervals whose rate or signal departs from the file}
\usage{
detect_time_anomalies(
  time,
  mat,
  n_bins = 40L,
  mad_k = 5,
  min_events_per_bin = 50L
)
}
\arguments{
\item{time}{numeric acquisition time per event, in the file's own units}

\item{mat}{matrix of channel values on the analysis scale, one column per
channel, same number of rows as \code{time}}

\item{n_bins}{number of equal-width time intervals}

\item{mad_k}{robust z above which a bin is flagged}

\item{min_events_per_bin}{bins holding fewer events than this are not judged
on their channel medians, because a median over a handful of events is
noise; they are still judged on their rate, which is the point}
}
\value{
list(bins = data.frame, flagged = logical per event, reason)
}
\description{
Flag acquisition intervals whose rate or signal departs from the file
}
