% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clustering.R
\name{run_unsupervised_clusters}
\alias{run_unsupervised_clusters}
\title{Unsupervised clustering of the embedded cells}
\usage{
run_unsupervised_clusters(
  cells,
  markers,
  n_clusters = 12L,
  grid = 10L,
  epochs = 10L,
  scale_method = "robust",
  seed = 42L,
  max_cells = 200000L
)
}
\arguments{
\item{cells}{embedding cell table}

\item{markers}{marker columns to cluster on (normally the UMAP feature set)}

\item{n_clusters}{number of metaclusters}

\item{grid}{SOM grid side length; grid^2 nodes}

\item{epochs}{Number of SOM training epochs. Default \code{10L}.}

\item{scale_method}{matched to run_umap()'s default so clusters and the
embedding see the same geometry}

\item{seed}{Random seed. The RNG stream is restored afterwards, so this cannot perturb later sampling. Default \code{42L}.}

\item{max_cells}{Ceiling on the number of cells used. Default \code{200000L}.}
}
\value{
list(cluster = integer per cell, method, codes, node)
}
\description{
WHY IT CLUSTERS THE MARKER MATRIX AND NOT THE UMAP COORDINATES: UMAP is a
visualisation. Its distances are not metric, it does not preserve density, and
the gaps between its islands are partly an artefact of min_dist. Clustering it
would give clusters of the PICTURE. Every established tool -- FlowSOM and
Phenograph among them -- clusters the high-dimensional space and uses the
embedding only to display the result, which is what this does.
}
