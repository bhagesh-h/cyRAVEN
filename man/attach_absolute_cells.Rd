% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/total-counts.R
\name{attach_absolute_cells}
\alias{attach_absolute_cells}
\title{Attach absolute cell numbers to a per-sample abundance table}
\usage{
attach_absolute_cells(ab, tc, share_col = "pct_of_gated")
}
\arguments{
\item{ab}{Abundance table with sample_id and a share column.}

\item{tc}{Matched total counts from \code{load_total_counts()}.}

\item{share_col}{Column holding the share, in percent.}
}
\value{
\code{ab} with total_cells, cells_absolute and count_basis added.
}
\description{
Multiplies each population share by that acquisition's total yield. Rows for
samples with no total are left NA rather than dropped, so the relative table
and the absolute table always have the same shape and the same row order and
can be read side by side.
}
\details{
WHY \code{cells_absolute} AND NOT A REPLACEMENT FOR pct_of_gated: the two answer
different questions and have different error. The share is measured once, by
this pipeline, on events it counted itself. The product carries the yield's
error as well, from an instrument this pipeline never saw. Overwriting the
share would hide that, so both travel together and the provenance column
says so.
}
