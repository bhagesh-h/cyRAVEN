% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stats-compositional.R
\name{stats_confounding}
\alias{stats_confounding}
\title{Is a covariate confounded with cohort, and does it track the outcome?}
\usage{
stats_confounding(
  freq,
  patients,
  smap,
  group_of,
  covariates = c("age", "sex"),
  value_col = NULL,
  min_n = 3L
)
}
\arguments{
\item{freq}{population_frequencies table}

\item{patients}{patient table (patient_id + the covariates)}

\item{smap}{sample map (sample_id -> patient_id)}

\item{group_of}{named vector sample_id -> cohort}

\item{covariates}{column names in \code{patients} to examine}

\item{value_col}{outcome column in \code{freq}; defaults to the abundance measure}

\item{min_n}{Minimum samples per group before a test is attempted. Default \code{3L}.}
}
\value{
data.frame; rows with population = NA carry the covariate-vs-cohort
balance test, the rest carry covariate-vs-outcome association
}
\description{
WHY SPEARMAN / KRUSKAL / FISHER: the same reasoning as everywhere else here --
small n, no free normality assumption. Numeric covariates get Spearman
(monotone, rank-based). Categorical covariates get Kruskal-Wallis against the
outcome and a Fisher exact test against cohort, which stays exact at the cell
counts a 2x3 sex-by-cohort table produces where chi-squared does not.
}
