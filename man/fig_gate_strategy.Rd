% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/convex-gates.R
\name{fig_gate_strategy}
\alias{fig_gate_strategy}
\title{Draw a proposed gating strategy, one panel per gate}
\usage{
fig_gate_strategy(
  X,
  y,
  strategy,
  outfile,
  label = "cluster",
  colors = fcs_colors(),
  dpi = 300
)
}
\arguments{
\item{X}{marker matrix used to derive the strategy}

\item{y}{0/1 target indicator}

\item{strategy}{the list returned by \code{\link[=explain_cluster]{explain_cluster()}}}

\item{outfile}{path to write to}

\item{label}{name of the population being explained}

\item{colors}{Named list of colours; defaults to the package palette. Default \code{fcs_colors()}.}

\item{dpi}{Raster resolution. Default \code{300}.}
}
\value{
the assembled plot, invisibly
}
\description{
WHY TARGETS ARE DRAWN LAST AND NON-TARGETS IN GREY: the question the reader
has is "does this polygon contain the population and exclude the rest", and
that is answered by seeing the population against the background it was
separated from. Colouring both categories equally makes a dense non-target
cloud hide the very cells the gate is about.
}
\details{
The subtitle carries the held-out metrics, not the in-sample ones, because
a figure is where an optimistic number does the most damage.
}
