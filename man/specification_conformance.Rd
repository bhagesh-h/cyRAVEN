% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/conformance.R
\name{specification_conformance}
\alias{specification_conformance}
\title{Test this run against a baseline}
\usage{
specification_conformance(
  thr_all,
  freq,
  spec,
  baseline,
  transform = "arcsinh",
  z_qualify = 3.5,
  z_fail = 6
)
}
\arguments{
\item{thr_all}{this run's thresholds table}

\item{freq}{this run's frequency table}

\item{spec}{this run's population specification}

\item{baseline}{the list from \code{\link[=read_spec_baseline]{read_spec_baseline()}}}

\item{transform}{this run's transform name}

\item{z_qualify}{robust z above which a marker is qualified}

\item{z_fail}{robust z above which a marker fails}
}
\value{
list(markers, populations, spec_changes, summary)
}
\description{
Compares each marker's threshold location against where the baseline put it,
scaled by the baseline's own spread for that marker, so the tolerance adapts to
how variable the marker legitimately is. The same comparison is made for
population frequencies.
}
\details{
THE SCALE IS FLOORED, for the reason it is floored in the within-run check: a
marker whose baseline samples happened to agree closely would otherwise divide
by near zero and fail on a difference of no consequence.

A CHANGED TRANSFORM INVALIDATES EVERY THRESHOLD COMPARISON, because thresholds
are expressed on the analysis scale. That case is reported as its own verdict
rather than as dozens of drifted markers.
}
