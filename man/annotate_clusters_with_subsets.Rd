% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/subsets.R
\name{annotate_clusters_with_subsets}
\alias{annotate_clusters_with_subsets}
\title{Annotate clusters with the immune subset their marker profile matches}
\usage{
annotate_clusters_with_subsets(prof, subsets = NULL, pos_cut = 0.5)
}
\arguments{
\item{prof}{Cluster profile: one row per cluster, with a \code{cluster} column and
one \verb{frac_pos.<marker>} column per marker, as written to
explore_cluster_profile.csv.}

\item{subsets}{Named list of subset definitions; defaults to the ones the
profile's own markers can resolve.}

\item{pos_cut}{Fraction of a cluster's cells that must be positive for a
marker before the cluster counts as positive for it.}
}
\value{
data.frame with one row per cluster: \code{cluster}, \code{subset_label},
\code{subset_markers} (the requirements that were checked), \code{n_requirements},
\code{margin} (the smallest distance from \code{pos_cut} across those requirements)
and \code{subset_alternatives}.
}
\description{
Annotate clusters with the immune subset their marker profile matches
}
\keyword{internal}
