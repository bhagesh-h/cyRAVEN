% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/thresholds.R
\name{sample_override}
\alias{sample_override}
\title{Look up a per-sample, per-marker threshold override}
\usage{
sample_override(overrides, sample_id, marker)
}
\arguments{
\item{overrides}{the \code{sample_overrides} list, or NULL}

\item{sample_id}{sample identifier}

\item{marker}{marker name}
}
\value{
a list carrying at least \code{threshold}, or NULL when none applies
}
\description{
Reads the \verb{sample_overrides:} block of the config, which is keyed on sample
identifier and then on marker:
}
\details{
\if{html}{\out{<div class="sourceCode yaml">}}\preformatted{sample_overrides:
  D07:
    CCR7:
      threshold: 2.15
      reason: "valley found inside the negative mode, see gating_qc.png"
      set_by: "initials or name"
}\if{html}{\out{</div>}}

Sample and marker names are matched exactly, as they appear in
\code{thresholds_used.csv}. A block naming a sample or marker that does not exist
is inert rather than an error, because a config is routinely reused across
cohorts that do not all contain the same tubes; unmatched entries are reported
once by \code{\link[=report_unused_overrides]{report_unused_overrides()}} rather than failing the run.
}
