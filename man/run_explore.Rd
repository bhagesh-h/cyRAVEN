% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/explore-run.R
\name{run_explore}
\alias{run_explore}
\title{Run explore mode}
\usage{
run_explore(
  reads,
  fpr,
  opt,
  outdir,
  transforms = NULL,
  gates = NULL,
  verdicts = NULL,
  pops = NULL,
  group_of = NULL,
  confounding = NULL,
  file_paths = character(0),
  total_counts = NULL
)
}
\arguments{
\item{reads}{Named list of reads.}

\item{fpr}{Panel resolution.}

\item{opt}{Option list.}

\item{outdir}{Run output directory.}

\item{transforms}{Per-panel transforms. Derived here if absent.}

\item{gates}{Per-sample gate objects, for thresholds and the viability marker.}

\item{verdicts}{Per-sample staining QC verdicts. Recorded, NOT used to
exclude: explore does not depend on the CD45 gate, so a sample the declared
path had to drop still contributes here. That is a capability rather than an
oversight, which is why it becomes a column instead of a filter.}

\item{pops}{Per-sample scored populations, for the bridge tables.}

\item{group_of}{Named character vector, sample id -> group.}

\item{confounding}{One-row data frame from the batch/group confounding check.}

\item{file_paths}{Full paths of the FCS files the run was given. Needed only
when the declared pipeline has already freed the event matrices, because
\code{reads[[s]]} records a basename and cannot be re-opened on its own.}
}
\value{
Invisibly, \code{list(dir, files, gaps)}. \code{gaps} is what explore has to say
about the declared specification -- populations spanning several clusters,
and clusters no population covers. The caller writes it to \code{spec_gaps.csv}
only under \code{--maybe-learn}; this function never writes outside \verb{explore/}.
}
\description{
Unsupervised discovery over every eligible channel, writing into
\verb{<outdir>/explore/}. Every argument beyond \code{reads}, \code{fpr}, \code{opt} and \code{outdir}
is optional: supplied, it makes the output better; absent, the run degrades to
what a standalone clusterer would produce and records that it did so in
\code{explore_provenance.csv}.
}
\keyword{internal}
