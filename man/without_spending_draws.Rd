% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/figures-total-counts.R
\name{without_spending_draws}
\alias{without_spending_draws}
\title{Run an expression with the global RNG stream left exactly as it was found}
\usage{
without_spending_draws(expr)
}
\arguments{
\item{expr}{Expression to evaluate.}
}
\description{
WHY THIS EXISTS. \code{run_cyraven()} seeds once and every later draw comes from
that one stream: the embedding's cell selection, the clustering, the
bootstraps. A step that spends draws therefore does not merely get its own
random numbers, it shifts every draw after it, and the visible symptom is
that adding an unrelated flag silently moves the UMAP. \code{geom_jitter()} draws,
so a figure is enough to do it -- on a two-panel explore run, drawing panel
1's figure would re-roll panel 2's embedding.
}
\details{
The package's contract is that a run without \code{--total-counts} produces
byte-identical output to one with it, apart from the files the flag adds.
Restoring the stream is what makes that true. Same idiom as
\code{clin_boot_ci()} and \code{run_flowsom()}.
}
\keyword{internal}
