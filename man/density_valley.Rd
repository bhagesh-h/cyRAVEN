% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/thresholds.R
\name{density_valley}
\alias{density_valley}
\title{Find the deepest density valley between two modes}
\usage{
density_valley(
  x,
  bins = 220L,
  smooth = 4,
  peak_frac = 0.02,
  min_gap_frac = 0.06,
  range_q = c(0.001, 0.999),
  min_rel_depth = 0.3,
  min_upper_frac = 0.002
)
}
\arguments{
\item{x}{A vector of values.}

\item{bins}{The bins. Default \code{220L}.}

\item{smooth}{The smooth. Default \code{4}.}

\item{peak_frac}{The peak frac. Default \code{0.02}.}

\item{min_gap_frac}{The min gap frac. Default \code{0.06}.}

\item{range_q}{The range q. Default \code{c(0.001, 0.999)}.}

\item{min_rel_depth}{minimum valley depth AS A FRACTION of the smaller of the
two flanking peaks. This is the parameter that makes minority positive
populations findable -- see the note on scoring below.}

\item{min_upper_frac}{the upper mode must hold at least this fraction of events,
so a cut cannot be placed out in the extreme tail where a handful of events
form a bump.}
}
\description{
WHAT: smooths a histogram, locates peaks above \code{peak_frac} of the maximum,
and returns the valley between the peak pair maximising depth * min(height).
Returns NA when the distribution is unimodal -- the honest answer, which then
routes threshold resolution to the unstained-control fallback.
WHY: for a bimodal marker the valley is the least-arbitrary cutoff available
and adapts to each sample's own staining intensity.
}
