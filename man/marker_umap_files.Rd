% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/report.R
\name{marker_umap_files}
\alias{marker_umap_files}
\title{The per-marker embeddings, pooled first and then split by category}
\usage{
marker_umap_files(outdir)
}
\arguments{
\item{outdir}{The run directory.}
}
\description{
WHY A HELPER. A run writes one figure per marker and then one per marker per
category, so the count is a property of the cohort rather than something a
section can name in advance. Returned as paths relative to \code{outdir}, which is
what \code{\link[=report_section]{report_section()}} and the catch-all both work in.
}
\details{
Ordering is deliberate: the pooled view of a marker comes before its splits,
so the reader sees the whole embedding before any comparison drawn on it.
}
\keyword{internal}
