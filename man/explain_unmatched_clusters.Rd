% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/convex-gates.R
\name{explain_unmatched_clusters}
\alias{explain_unmatched_clusters}
\title{Propose gating strategies for clusters the spec does not describe}
\usage{
explain_unmatched_clusters(
  cells,
  cluster,
  features,
  agreement,
  max_clusters = 4L,
  min_cells = 200L,
  purity_max = 80,
  ...
)
}
\arguments{
\item{cells}{embedding cell table carrying the marker columns}

\item{cluster}{integer vector from \code{\link[=run_unsupervised_clusters]{run_unsupervised_clusters()}}}

\item{features}{marker columns to gate on}

\item{agreement}{the list returned by \code{\link[=cluster_gate_agreement]{cluster_gate_agreement()}}}

\item{max_clusters}{ceiling on how many strategies to derive}

\item{min_cells}{smallest cluster worth explaining}

\item{purity_max}{only explain clusters no cleaner than this (percent)}

\item{...}{passed to \code{\link[=explain_cluster]{explain_cluster()}}}
}
\value{
list(summary, polygons, strategies), or NULL
}
\description{
WHAT IT IS FOR. \code{\link[=cluster_gate_agreement]{cluster_gate_agreement()}} identifies clusters whose cells
carry no population label, or whose dominant label covers only part of them.
Those are the clusters worth explaining: the spec has nothing to say about
them, so there is no gate to inspect and no threshold to blame. This runs
\code{\link[=explain_cluster]{explain_cluster()}} on each and writes out a strategy a cytometrist can draw.
}
\details{
WHY IT ONLY TAKES THE FIRST FEW. Each strategy costs a handful of small
optimisations, which is cheap, but a report proposing fifteen new gates is
not a finding, it is a second problem. \code{max_clusters} keeps the output to the
clusters with the strongest claim to being real populations -- the largest
undescribed ones -- and the count of what was left out is logged rather than
passed over in silence.
}
