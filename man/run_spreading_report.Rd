% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spreading.R
\name{run_spreading_report}
\alias{run_spreading_report}
\title{Spreading across a cohort, and which markers it is costing}
\usage{
run_spreading_report(
  pops,
  gates,
  thr_all = NULL,
  panel_of = NULL,
  max_samples = 8L,
  flag_ratio = 1.25
)
}
\arguments{
\item{pops}{per-sample scoring results, carrying \code{tmat} and \code{thresholds}}

\item{gates}{per-sample gate objects}

\item{thr_all}{the thresholds table, used for the fallback rate per marker}

\item{panel_of}{named character vector mapping sample to panel}

\item{max_samples}{cap on samples contributing, since the ranking stabilises
long before every sample is used}

\item{flag_ratio}{spreading ratio at or above which a pair is called
substantial}
}
\value{
list(pairs, receivers), each a data.frame, or NULL
}
\description{
Aggregates \code{\link[=spreading_pairs]{spreading_pairs()}} over samples and joins the result to how each
receiver's threshold was obtained. A marker that receives a great deal of
spreading AND falls back to a quantile is the actionable finding: its cut is
unresolved for a reason the panel can fix.
}
